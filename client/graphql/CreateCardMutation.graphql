mutation CreateCard($input: CardInput!) {
  createCard(input: $input) {
    status
    card {
      id
      title
      content
      category
      user
      # image
      createdAt
      updatedAt
    }
  }
}
